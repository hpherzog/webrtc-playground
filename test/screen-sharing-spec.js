

import * as screenSharing from '../lib/media/screen-sharing';
import {BrowserNotSupported} from '../lib/errors';

describe("Screen sharing", function(){

    it('should create a media stream', function(done){

        /*
        this.timeout(10000);

        screenSharing.setBrowserConfig('chrome', {
            extensionId: 'pokjackiolcbbbganchnfnocfjfiaigf'
        });

        screenSharing.createStream().then((stream)=>{
            console.log(stream);
            done();
        }).catch((err)=>{
            if(err instanceof BrowserNotSupported) {
                console.log(err);
                done();
            } else {
                done(err);
            }
        });
        */
    })
});
